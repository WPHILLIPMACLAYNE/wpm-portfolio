# 11 - MD Closure 2026-05-06

Fechamento dos Markdown após a rodada de hardening local do WPM.OS.

## Escopo Fechado

- Auditoria profissional: findings `AUD-001` a `AUD-012` permanecem fechados no working tree.
- Auditoria UX: itens executáveis sem serviço externo foram reconciliados e marcados como `closed`, `blocked` ou `deferred`.
- Creative spec/brief: checklists antigos foram alinhados ao estado real do app.
- Technical reference: comandos, acessibilidade, performance e export GitHub Pages foram atualizados.
- README/overview: passam a apontar para este fechamento e para a separação entre local pronto e publicação externa.

## Correções Aplicadas Nesta Rodada

- Contexto de rota e destaque de navegação ativa no shell.
- Botão mobile fixo `Back` nas páginas internas.
- Live region para transições da Home.
- Drawer/ribbon mobile com alvos maiores e duas colunas para módulos.
- Tamanho mínimo de texto elevado para 11px e tracking excessivo reduzido.
- Scanlines desligados no mobile.
- Estados indisponíveis recebem descrição semântica.

## Bloqueios Não Técnicos

- Email público: depende de decisão do autor.
- Formulário de contato: depende de provedor, política de privacidade e secrets aprovados.
- GitHub Pages: aprovado e ativo via workflow `.github/workflows/pages.yml`.
- Outros serviços externos: dependem de aprovação explícita antes de ativar.
- Testes manuais assistivos/cross-browser reais: NVDA/VoiceOver, Safari/Firefox, iPhone/iPad físico e slow 3G seguem como QA manual externo.

## Validação Esperada

```bash
npm run lint
npm run typecheck
npm run build
npm run build:github-pages
npm audit --audit-level=low
npm run test:e2e
```

## Estado

Localmente, o máximo possível está fechado e o GitHub Pages foi autorizado.
Próxima etapa operacional é validar a publicação pública e executar QA manual em
dispositivos/navegadores reais.
